from setuptools import setup

setup(
    name="sign_in_ocr",
    version="0.1",
    description="OCR for bulk name parsing repurposed for RecordSponge",
    packages=["sign_in_ocr"],
    package_dir={"": "."},
)
